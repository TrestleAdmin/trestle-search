require 'spec_helper'

describe Trestle::Search do
  it 'has a version number' do
    expect(Trestle::Search::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
